🎯 **What:**
Added a test to `useWorkoutData.test.ts` to verify the hook's resiliency against malformed JSON payloads in workout definitions. Addressed a pre-existing test failure within `worker.test.ts` that occurred due to passing `undefined` to `createRequest` in a mock test payload rather than a valid body (`{}`).

📊 **Coverage:**
- `useWorkoutData` JSON parse fail-safes are now covered by unit tests, verifying that parsing failures do not crash the hook or halt operations.
- Covered edge case: Mocked `workoutService.fetchWorkouts` returning invalid JSON in the `notes` field while syncing definitions.

✨ **Result:**
- Test suite is 100% green without regressions (`tsc --noEmit && pnpm test`).
- Safe ignoring of badly formatted cloud notes guarantees that future UI interactions won't break if bad data exists.
